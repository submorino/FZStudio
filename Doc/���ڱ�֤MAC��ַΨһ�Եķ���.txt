2011-11-3

1.保证MAC地址唯一性的方法：
  1)本地生成策略：
		由本地算法生成MAC地址，保证在本地账户中唯一。
		优势：不依赖网络和服务器。
		缺点：算法要求高，否则无法保证与其他机器的唯一性。
		
  2)数据库分配侧率：
		由工具生成MAC地址数据库，导入到数据服务器，程序主动访问数据服务器取得MAC地址。
		优势：可以保证局域网内MAC地址唯一性。
		缺点：依赖网络和服务器的健壮性，需人工参与。
  
2.